<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".EduSyncApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduSync"
        android:usesCleartextTraffic="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activities.FormularioActivity"
            android:exported="false" />
        <activity android:name=".activities.DetalleEventoActivity" android:exported="false" />
        <activity android:name=".activities.AjustesActivity" />
        <activity android:name=".activities.AdminActivity" android:exported="false" />

        <activity
            android:name=".activities.PerfilClaseActivity"
            android:exported="false"
            android:theme="@style/Theme.EduSync" />
        <activity
            android:name=".activities.ListaParticipantesActivity"
            android:exported="false"
            android:theme="@style/Theme.EduSync" />
        <activity
            android:name=".activities.EditarPerfilActivity"
            android:exported="false"
            android:theme="@style/Theme.EduSync" />
        <activity
            android:name=".activities.ListaTareasActivity"
            android:exported="false"
            android:theme="@style/Theme.EduSync" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".NotificacionReceiver" android:exported="false" />
        <service android:name=".services.PomodoroService" android:exported="false" />

        <!-- Agenda Widget -->
        <receiver
            android:name=".widget.EduSyncWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/edusync_widget_info" />
        </receiver>

        <service
            android:name=".widget.EduSyncWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>

</manifest>
